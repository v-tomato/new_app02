<header class="masthead2">
</header>  
<div class = "calendar-1">
<%= month_calendar events: @meetings do |date, meetings| %>
  <%= date %>

  <% meetings.each do |meeting| %>
    <div>
      <%= link_to meeting.name, new_meeting %>
    </div>
  <% end %>
<% end %>
</div>

